# rules.conf — window rules, layer rules (+ security template). Later rule wins on conflicts.
# Docs: window-rules https://docs.noctalia.dev/umbriel/window-rules/
#       layer-rules  https://docs.noctalia.dev/umbriel/layer-rules/
#       security     https://docs.noctalia.dev/umbriel/security/

# Gaming template for Vega 5600G + 60Hz SDR (S19C170 has no VRR/HDR).
# Uncomment for lowest latency on fullscreen games (requires output tearing=true):
# [[window_rule]]
# match.content_type = "game"
# vrr = "disabled"
# tearing = true
# hdr = "off"
# For Proton/Wine fullscreen: match.xdg_tag = "^proton-game$" + default_fullscreen = true

[[window_rule]]
blur = true
opacity = 0.85

[[window_rule]]
match.app_id = "blender"
default_floating = false

[[window_rule]]
match.app_id = "brave-browser"
default_floating = false

[[window_rule]]
match.app_id = "org.gnome.Loupe"
default_floating = true

[[window_rule]]
match.app_id = "kvantummanager|qt5ct|qt6ct|nwg-look|org.kde.ark|org.pulseaudio.pavucontrol|blueman-manager"
default_floating = true

[[window_rule]]
match.title = "^Open$|^Save As$|^Choose Files$|^File Operation Progress$"
default_floating = true

[[window_rule]]
match.app_id = "^dev.noctalia.UmbrielSharePicker$"
default_floating = false
default_size = [800, 600]

# --- Layer rules (run `umbriel layers` for live namespaces) ---
[[layer_rule]]
match.namespace = "^noctalia-(bar-[^\"]+|notification|dock|panel|attached-panel|osd|desktop-widget-[^\"]*)$"
blur = true
blur_ignore_alpha = 0.5
blur_optimized = false

# --- Security context (sandboxed clients). Uncomment only for apps that need it. ---
# [[security_context_rule]]
# match.sandbox_engine = 'org\.flatpak'
# match.app_id = 'org\.example\.ClipboardManager'
# allow_globals = ["ext_data_control_manager_v1", "zwlr_data_control_manager_v1"]
